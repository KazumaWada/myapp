<div class="text-left mt-4">
  <h4><%= @user.name %>の<%= @title %>一覧</h4>
  <% if @users.any? %>   
    <%= render @users %>
    <% else %>
    まだ<%= @title %>がいません。
  <% end %>
</div>

<%# <% @posts_get_likes.each do |post_get_like| %>
<%# <% end %> 

