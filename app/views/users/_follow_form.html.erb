<%# ユーザーをフォローする際のボタン %>
<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
   <%#_unfollow.html.erb %>
    <%= render 'unfollow' %>
  <% else %>
   <%#_follow.html.erb %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>