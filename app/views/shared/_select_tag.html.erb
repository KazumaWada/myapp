 <%= form_tag posts_path, method: :get, class: 'nav justify-content-center' do %>
      <%= select_tag :tag_id,
                     options_from_collection_for_select(Tag.all, :id, :name, params[:tag_id]),
                     {
                       prompt: 'タグ一覧',
                       onchange: 'submit(this.form);',
                       class: 'form-control'
                     }
      %>
    <% end %>