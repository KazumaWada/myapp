<% if @contents_feed.any? %>
  <ol class="posts">
    <%= render @contents_feed %>
  </ol>
  #投稿ではなく、個人の投稿の塊のpaginateであることに注意。
   #これは、peginateをこちらの指示で場所を明確にする時の書き方。homeに表示したいから。
   #でも、みんなの投稿をhomeにしたいから、これは使わないと思う。
  <%# <%= will_paginate @feed_items,
                    params: { controller: :pafe, action: :homr } %> 
  各＠contents＿feedが、一つ一つの投稿が、postClassを持っているから、Postのパーシャルを呼び出す事ができる。
<% end %>
